---
import Layout from '../layouts/Layout.astro';

const courses = [
  {
    title: "Emotional Processing 101",
    description: "Learn to identify, understand, and work through difficult emotions without getting overwhelmed.",
    outcomes: [
      "Understand the purpose of different emotions",
      "Develop tools for processing intense feelings",
      "Build a sustainable emotional wellness practice",
    ],
    featured: true,
  },
  {
    title: "A Guide to Stress-Free Living",
    description: "Practical strategies for managing stress and building resilience in daily life.",
    outcomes: [
      "Identify your personal stress patterns",
      "Learn evidence-based stress reduction techniques",
      "Create a personalized stress management plan",
    ],
  },
];
---

<Layout title="Courses" description="Self-guided courses and tools for personal growth from Katie d'Autremont, LCPC.">
  <!-- Hero -->
  <section class="py-24 px-8 relative overflow-hidden">
    <div class="blob-sage w-80 h-80 -top-40 -right-40 absolute"></div>
    <div class="blob-sage w-64 h-64 bottom-0 -left-32 absolute"></div>

    <div class="max-w-3xl mx-auto relative">
      <div class="text-center mb-16">
        <div class="accent-line mx-auto mb-6"></div>
        <h1 class="text-4xl md:text-5xl text-charcoal mb-6">Self-Guided Tools</h1>
        <p class="text-lg text-charcoal-light max-w-2xl mx-auto">
          Courses and guides for working through specific challenges at your own pace.
        </p>
      </div>

      <!-- Featured Course -->
      {courses.filter(c => c.featured).map(course => (
        <div class="bg-gradient-to-br from-sage/10 via-sage/5 to-transparent rounded-xl p-12 mb-12 border border-sage-light relative overflow-hidden">
          <div class="absolute -top-20 -right-20 w-40 h-40 rounded-full bg-sage/10 blur-3xl"></div>
          <div class="relative">
            <span class="inline-block text-xs text-sage font-medium uppercase tracking-wide bg-sage/10 px-3 py-1 rounded-full">Start Here</span>
            <h2 class="font-serif text-2xl text-charcoal mt-4 mb-6">{course.title}</h2>
            <p class="text-charcoal-light mb-8">{course.description}</p>
            <div class="mb-8">
              <p class="text-sm font-medium text-charcoal mb-4">What you'll learn:</p>
              <ul class="space-y-4">
                {course.outcomes.map(outcome => (
                  <li class="flex items-start gap-4 text-sm text-charcoal-light">
                    <svg class="w-5 h-5 text-sage flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    <span>{outcome}</span>
                  </li>
                ))}
              </ul>
            </div>
            <button
              class="bg-sage text-cream px-8 py-4 rounded-md hover:bg-sage-dark transition-colors font-medium opacity-75 cursor-not-allowed"
              disabled
            >
              Coming Soon
            </button>
          </div>
        </div>
      ))}

      <!-- Course Grid -->
      <div class="space-y-6 mb-12">
        {courses.filter(c => !c.featured).map(course => (
          <div class="bg-white rounded-xl p-8 border border-cream-dark hover:border-sage-light hover:shadow-md transition-all">
            <h3 class="font-serif text-xl text-charcoal mb-4">{course.title}</h3>
            <p class="text-charcoal-light mb-6">{course.description}</p>
            <div class="mb-6">
              <p class="text-sm font-medium text-charcoal mb-4">What you'll learn:</p>
              <ul class="space-y-2">
                {course.outcomes.map(outcome => (
                  <li class="flex items-start gap-4 text-sm text-charcoal-light">
                    <svg class="w-4 h-4 text-sage flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    <span>{outcome}</span>
                  </li>
                ))}
              </ul>
            </div>
            <span class="text-warm-gray font-medium text-sm">
              Coming Soon
            </span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="py-16 px-8 bg-white">
    <div class="max-w-3xl mx-auto">
      <h2 class="font-serif text-2xl mb-8 text-center">Questions</h2>
      <div class="space-y-4">
        <details class="bg-cream/50 rounded-xl border border-cream-dark group">
          <summary class="p-6 cursor-pointer font-medium text-charcoal hover:text-sage transition-colors flex items-center justify-between">
            Are these a substitute for therapy?
            <svg class="w-5 h-5 text-warm-gray group-open:rotate-180 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </summary>
          <div class="px-6 pb-6 text-sm text-charcoal-light">
            <p>No. These courses are educational tools for personal growth. They're not therapy and aren't a substitute for professional mental health treatment. If you're struggling with significant mental health challenges, please reach out to a licensed provider.</p>
          </div>
        </details>
        <details class="bg-cream/50 rounded-xl border border-cream-dark group">
          <summary class="p-6 cursor-pointer font-medium text-charcoal hover:text-sage transition-colors flex items-center justify-between">
            How long do I have access?
            <svg class="w-5 h-5 text-warm-gray group-open:rotate-180 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </summary>
          <div class="px-6 pb-6 text-sm text-charcoal-light">
            <p>Once you purchase a course, you'll have lifetime access to the materials.</p>
          </div>
        </details>
        <details class="bg-cream/50 rounded-xl border border-cream-dark group">
          <summary class="p-6 cursor-pointer font-medium text-charcoal hover:text-sage transition-colors flex items-center justify-between">
            Can I get a refund?
            <svg class="w-5 h-5 text-warm-gray group-open:rotate-180 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </summary>
          <div class="px-6 pb-6 text-sm text-charcoal-light">
            <p>Yes. If a course isn't right for you, reach out within 14 days for a full refund.</p>
          </div>
        </details>
      </div>
    </div>
  </section>
</Layout>
