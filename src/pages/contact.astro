---
import Layout from '../layouts/Layout.astro';
import { url } from '../utils/url';
---

<Layout title="Contact" description="Get in touch with Katie d'Autremont for therapy inquiries, speaking engagements, or general questions.">
  <!-- Hero -->
  <section class="py-24 px-8 relative overflow-hidden">
    <div class="blob-sage w-80 h-80 -top-40 -right-40 absolute"></div>
    <div class="blob-sage w-64 h-64 bottom-0 -left-32 absolute"></div>

    <div class="max-w-2xl mx-auto relative">
      <div class="text-center mb-12">
        <div class="accent-line mx-auto mb-6"></div>
        <h1 class="text-4xl md:text-5xl text-charcoal mb-6">Get in Touch</h1>
        <p class="text-lg text-charcoal-light max-w-xl mx-auto">
          Whether you're interested in therapy, have a question, or just want to say hello.
        </p>
      </div>

      <!-- Contact Form -->
      <form class="bg-white rounded-xl p-12 border border-cream-dark shadow-sm mb-12">
        <div class="grid md:grid-cols-2 gap-6 mb-8">
          <div>
            <label for="name" class="block text-sm font-medium text-charcoal mb-4">
              Name
            </label>
            <input
              type="text"
              id="name"
              name="name"
              class="w-full px-6 py-4 rounded-md border border-cream-dark bg-cream/30 focus:border-sage focus:outline-none focus:bg-white transition-colors"
              required
            />
          </div>
          <div>
            <label for="email" class="block text-sm font-medium text-charcoal mb-4">
              Email
            </label>
            <input
              type="email"
              id="email"
              name="email"
              class="w-full px-6 py-4 rounded-md border border-cream-dark bg-cream/30 focus:border-sage focus:outline-none focus:bg-white transition-colors"
              required
            />
          </div>
        </div>

        <div class="mb-8">
          <label for="subject" class="block text-sm font-medium text-charcoal mb-4">
            What's this about?
          </label>
          <select
            id="subject"
            name="subject"
            class="w-full px-6 py-4 rounded-md border border-cream-dark bg-cream/30 focus:border-sage focus:outline-none focus:bg-white transition-colors"
            required
          >
            <option value="">Select one...</option>
            <option value="therapy">Therapy inquiry</option>
            <option value="courses">Question about courses</option>
            <option value="speaking">Speaking or collaboration</option>
            <option value="other">Something else</option>
          </select>
        </div>

        <div class="mb-8">
          <label for="message" class="block text-sm font-medium text-charcoal mb-4">
            Message
          </label>
          <textarea
            id="message"
            name="message"
            rows="5"
            class="w-full px-6 py-4 rounded-md border border-cream-dark bg-cream/30 focus:border-sage focus:outline-none focus:bg-white transition-colors resize-none"
            required
          ></textarea>
        </div>

        <button
          type="submit"
          class="w-full bg-sage text-cream py-6 rounded-md hover:bg-sage-dark transition-colors font-medium"
        >
          Send Message
        </button>
      </form>

      <!-- Additional Info -->
      <div class="grid md:grid-cols-2 gap-6">
        <div class="bg-sage/5 p-8 rounded-xl border border-sage-light/30 hover:border-sage-light transition-colors">
          <div class="w-10 h-10 rounded-full bg-sage/10 flex items-center justify-center mb-4">
            <svg class="w-5 h-5 text-sage" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
            </svg>
          </div>
          <h2 class="font-serif text-lg text-charcoal mb-3">For Therapy Inquiries</h2>
          <p class="text-sm text-charcoal-light mb-4">
            If you're interested in starting therapy, I offer a free 15-minute consultation to see if we're a good fit.
          </p>
          <a href={url('/therapy')} class="text-sage font-medium text-sm hover:text-sage-dark transition-colors inline-flex items-center gap-1">
            Learn more about therapy
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
        <div class="bg-cream-dark/50 p-8 rounded-xl border border-cream-dark hover:border-sage-light/50 transition-colors">
          <div class="w-10 h-10 rounded-full bg-sage/10 flex items-center justify-center mb-4">
            <svg class="w-5 h-5 text-sage" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <h2 class="font-serif text-lg text-charcoal mb-3">Response Time</h2>
          <p class="text-sm text-charcoal-light">
            I typically respond within 2-3 business days. If your inquiry is urgent, please indicate that in your message.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Telehealth Note -->
  <section class="py-12 px-8 bg-white">
    <div class="max-w-2xl mx-auto">
      <div class="p-8 bg-gradient-to-br from-sage/5 via-sage/10 to-sage/5 rounded-xl border-l-4 border-sage">
        <p class="text-sm text-charcoal-light">
          <strong class="text-charcoal">Note:</strong> All therapy sessions are conducted via secure telehealth video.
          I am currently licensed to see clients in <strong class="text-charcoal">[State(s)]</strong>.
        </p>
      </div>
    </div>
  </section>
</Layout>
